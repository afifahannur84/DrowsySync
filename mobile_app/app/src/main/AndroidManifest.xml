<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Network ── -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ── Foreground Service (required to call startForegroundService) ── -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- ── Foreground Service Type: DATA_SYNC (required on API 34+) ── -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- ── Full-Screen Intent (pop over other apps / lock screen) ── -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- ── Post Notifications (required on Android 13 / API 33+) ── -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ── Wake lock (keeps CPU alive while service polls) ── -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/drowsysync_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/drowsysync_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.DrowsySyncApp"
        android:usesCleartextTraffic="true">

        <!-- ── Launcher: Dashboard (routes to Login if not logged in) ── -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── Login ── -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- ── Registration ── -->
        <activity
            android:name=".RegistrationActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".EditProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.DrowsySyncApp" />

        <!-- ── Email Verification ── -->
        <activity
            android:name=".EmailVerificationActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- ── History & Analytics ── -->
        <activity
            android:name=".HistoryActivity"
            android:exported="false" />

        <!-- ── Change Ownership ── -->
        <activity
            android:name=".ChangeOwnerActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- ── Symptom Alert Overlay (semi-transparent) ── -->
        <!-- showWhenLocked + turnScreenOn = pops over other apps and lock screen -->
        <activity
            android:name=".SymptomAlertActivity"
            android:exported="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.DrowsySyncApp.Transparent" />

        <!-- ── Microsleep Emergency ── -->
        <!--     showWhenLocked + turnScreenOn = pops over lock screen         -->
        <!--     exported="true" required so the foreground service can launch it -->
        <activity
            android:name=".MicrosleepAlertActivity"
            android:exported="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true" />

        <!-- ── DrowsySync Background Polling Service ── -->
        <!--     foregroundServiceType="dataSync" required on API 34+           -->
        <service
            android:name=".DrowsySyncBackgroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>
</manifest>