<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DrowsySyncApp">

        <!-- ── Launcher: Dashboard (routes to Registration if not logged in) ── -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── Registration ── -->
        <activity
            android:name=".RegistrationActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- ── Email Verification ── -->
        <activity
            android:name=".EmailVerificationActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- ── History & Analytics ── -->
        <activity
            android:name=".HistoryActivity"
            android:exported="false" />

        <!-- ── Change Ownership ── -->
        <activity
            android:name=".ChangeOwnerActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- ── Symptom Alert Overlay (transparent) ── -->
        <activity
            android:name=".SymptomAlertActivity"
            android:exported="false"
            android:theme="@style/Theme.DrowsySyncApp.Transparent" />

        <!-- ── Microsleep Emergency ── -->
        <activity
            android:name=".MicrosleepAlertActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true" />

    </application>
</manifest>