# =============================================================================
# requirements_laptop.txt — Windows / Laptop Dependencies
# DrowsySync Vision Client
# =============================================================================
# Setup:
#   cd vision_client
#   python -m venv venv
#   .\venv\Scripts\activate
#   pip install -r requirements_laptop.txt
# =============================================================================

opencv-python>=4.8.0,<5.0.0
mediapipe==0.10.14   # 0.10.15+ dropped mp.solutions — pin to last working version
numpy>=1.24.0,<2.0.0
pygame>=2.5.0

# Uncomment when connecting to cloud backend:
# requests>=2.31.0
